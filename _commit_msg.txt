fix: ObjectId 序列化 + Chat SSE 路径 + LLM 配置

## 修复内容

1. **ObjectId 序列化（500 错误）**
   - main.py 添加 MongoJSONResponse 全局响应类，自动将 ObjectId 转为字符串
   - resume_service 的 list/get_detail 添加 {"_id": 0} 投影
   - 修复简历列表 500 错误

2. **Chat SSE 路径（404 错误）**
   - 前端 chat.ts 的 sendMessageStream URL 从 /messages/stream 改为 /messages
   - sseChat fetch 请求补全 /api/v1 前缀
   - 匹配后端路由 POST /chat/sessions/{id}/messages

3. **LLM 配置**
   - .env 配置 DeepSeek API（deepseek-chat 模型）
   - test_config.py 适配新模型名

## 验证
- 后端 152 测试通过
- 简历列表 API 验证通过
- 会话创建/列表 API 验证通过
