# 认证与权限控制实施计划

**Goal:** 为 HR 系统添加注册功能、RBAC 权限控制、用户管理（审批/启用禁用/重置密码/删除）。

**Architecture:** 复用现有 JWT 体系，扩展 users 表 status 字段（pending/approved/disabled）和 role 字段（admin/hr）。后端通过 FastAPI 依赖注入 `require_admin` 实现权限校验，启动时自动初始化管理员。前端新增用户管理页（仅 admin 可见）和登录页申请账号入口。

**Tech Stack:** FastAPI + MongoDB + bcrypt + JWT（后端）/ Vue 3 + Pinia + Element Plus（前端）

## Global Constraints

- Python 3.12 + `.venv\Scripts\python.exe`，测试用 `.venv\Scripts\python.exe -m pytest`
- API 路由前缀 `/api/v1/[module]`，统一响应 `{code, message, data, trace_id}`
- 不修改基础代码环境，依赖锁定在 requirements.txt / package.json
- 配置分离：敏感信息走 `.env`
- 每个文件头部含元信息（文件名/创建时间/作者/功能描述）
- 每个函数/类含文档注释
- 模块化设计，单文件单职责

## File Structure

### 后端新增
- `app/models/user.py` — 用户相关 Pydantic 模型
- `app/services/user_service.py` — 用户管理业务逻辑
- `app/api/users.py` — 用户管理路由
- `tests/services/test_user_service.py` — UserService 单元测试
- `tests/api/test_users_api.py` — users 路由 API 测试

### 后端修改
- `app/core/config.py` — 新增 ADMIN_USERNAME/PASSWORD/EMAIL 配置
- `app/core/response.py` — 新增业务错误码
- `app/core/database.py` — users 表索引
- `app/core/exceptions.py` — 复用 AuthError（已有）
- `app/services/auth_service.py` — register / login status / change_password
- `app/api/auth.py` — register / password 路由
- `app/api/deps.py` — require_admin 依赖
- `app/main.py` — lifespan 加 _ensure_admin + 挂载 users 路由
- `app/api/resumes.py` — delete 加 require_admin
- `app/api/email.py` — config 接口加 require_admin
- `.env.example` — 新增配置项

### 前端新增
- `src/types/user.ts` — 用户类型定义
- `src/api/user.ts` — 用户管理 API
- `src/components/auth/RegisterDialog.vue` — 注册申请对话框
- `src/components/user/UserFormDialog.vue` — 用户表单对话框（创建/重置密码）
- `src/views/UserList.vue` — 用户管理页

### 前端修改
- `src/types/auth.ts` — role/status 类型细化
- `src/api/auth.ts` — register / changePassword
- `src/stores/auth.ts` — 无改动（user 已含 role）
- `src/views/Login.vue` — 加"申请账号"链接
- `src/views/Layout.vue` — admin 菜单项 + 管理员标识
- `src/router/index.ts` — /users 路由 + 守卫

---

## Task 1: 后端配置与错误码扩展

**Files:**
- Modify: `backend/app/core/config.py`
- Modify: `backend/app/core/response.py`
- Modify: `backend/.env.example`

**Interfaces:**
- Produces: settings.ADMIN_USERNAME / ADMIN_PASSWORD / ADMIN_EMAIL / CODE.AUTH_PENDING / AUTH_DISABLED / USER_NOT_FOUND / USERNAME_EXISTS / FORBIDDEN

- [ ] **Step 1: config.py 新增管理员配置**

修改 `backend/app/core/config.py`，在 `class Settings` 末尾（`class Config` 之前）添加：

```python
    # 管理员初始化（启动时自动创建）
    ADMIN_USERNAME: str = "admin"
    ADMIN_PASSWORD: str = "your-secure-password-here"
    ADMIN_EMAIL: str = "admin@test.com"
```

- [ ] **Step 2: response.py 新增错误码**

修改 `backend/app/core/response.py`，在 `class CODE` 中追加（保持与现有码段连续）：

```python
    # 认证扩展（1003-1005）
    AUTH_PENDING = 1003       # 账号待审批
    AUTH_DISABLED = 1004      # 账号已禁用
    FORBIDDEN = 1005          # 权限不足
    # 用户管理（2001-2002）
    USER_NOT_FOUND = 2001     # 用户不存在
    USERNAME_EXISTS = 2002    # 用户名已存在
```

- [ ] **Step 3: .env.example 新增配置**

修改 `backend/.env.example`，在末尾追加：

```bash
# 管理员初始化（启动时自动创建）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here
ADMIN_EMAIL=admin@test.com
```

- [ ] **Step 4: 提交**

```bash
git add backend/app/core/config.py backend/app/core/response.py backend/.env.example
git commit -m "feat(auth): 新增管理员初始化配置与权限错误码"
```

---

## Task 2: users 表索引

**Files:**
- Modify: `backend/app/core/database.py:22-37`

**Interfaces:**
- Produces: users 表 username 唯一索引 + role/status 复合索引

- [ ] **Step 1: database.py 增加 users 索引**

修改 `backend/app/core/database.py` 的 `connect` 方法，在 `await cls.db.email_config.create_index("config_id", unique=True)` 之后、`logger.info("MongoDB 已连接", extra={})` 之前追加：

```python
        # users 表索引
        await cls.db.users.create_index("username", unique=True)
        await cls.db.users.create_index([("role", 1), ("status", 1)])
```

- [ ] **Step 2: 启动验证（手动）**

后端启动确认无错误（索引创建成功）。后续 Task 会统一跑测试。

- [ ] **Step 3: 提交**

```bash
git add backend/app/core/database.py
git commit -m "feat(auth): users 表新增 username 唯一索引与 role/status 复合索引"
```

---

## Task 3: 用户模型

**Files:**
- Create: `backend/app/models/user.py`

**Interfaces:**
- Produces: RegisterRequest / CreateUserRequest / UpdateStatusRequest / ResetPasswordRequest / ChangePasswordRequest / UserItem / UserListResponse

- [ ] **Step 1: 创建 user.py 模型文件**

创建 `backend/app/models/user.py`：

```python
"""
文件名: app/models/user.py
创建时间: 2026-06-27
作者: TalentSense Team
功能描述: 用户管理相关 Pydantic 模型
入参/出参: 注册/创建/状态更新/密码重置请求体，用户列表项
"""
from pydantic import BaseModel, Field, EmailStr
from typing import Literal


class RegisterRequest(BaseModel):
    """HR 自助注册请求（status=pending）"""
    username: str = Field(..., min_length=3, max_length=20, description="用户名")
    password: str = Field(..., min_length=8, max_length=32, description="密码")
    email: str | None = Field(default=None, description="邮箱（可选）")
    name: str | None = Field(default=None, max_length=30, description="显示名（可选）")


class CreateUserRequest(BaseModel):
    """管理员直接开号请求（status=approved）"""
    username: str = Field(..., min_length=3, max_length=20)
    password: str = Field(..., min_length=8, max_length=32)
    email: str | None = None
    name: str | None = Field(default=None, max_length=30)
    role: Literal["admin", "hr"] = Field(default="hr")


class UpdateStatusRequest(BaseModel):
    """启用/禁用账号"""
    status: Literal["approved", "disabled"]


class ResetPasswordRequest(BaseModel):
    """管理员重置密码"""
    new_password: str = Field(..., min_length=8, max_length=32)


class ChangePasswordRequest(BaseModel):
    """用户修改自己密码"""
    old_password: str = Field(..., min_length=1)
    new_password: str = Field(..., min_length=8, max_length=32)


class UserItem(BaseModel):
    """用户列表项"""
    user_id: str
    username: str
    email: str | None = None
    name: str | None = None
    role: str
    status: str
    created_at: str
    updated_at: str
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/models/user.py
git commit -m "feat(auth): 新增用户管理 Pydantic 模型"
```

---

## Task 4: AuthService 扩展（register / login status / change_password）

**Files:**
- Modify: `backend/app/services/auth_service.py`
- Modify: `backend/tests/services/test_auth_service.py`

**Interfaces:**
- Consumes: settings.ADMIN_* / users_coll / hash_password / verify_password
- Produces: register() / change_password() / login 增加 status 校验

- [ ] **Step 1: 编写 register / login status / change_password 测试**

修改 `backend/tests/services/test_auth_service.py`，追加（保留现有测试不动）：

```python
import pytest
from unittest.mock import AsyncMock, MagicMock
from app.services.auth_service import AuthService
from app.core.exceptions import AuthError


@pytest.mark.asyncio
async def test_register_success():
    """注册成功，status=pending, role=hr"""
    svc = AuthService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=None)
    svc.users_coll.insert_one = AsyncMock(return_value=MagicMock(inserted_id="oid"))
    result = await svc.register(username="zhangsan", password="Pass1234", email="z@test.com", name="张三")
    assert result["username"] == "zhangsan"
    assert result["status"] == "pending"
    # 校验插入的文档字段
    insert_call = svc.users_coll.insert_one.call_args
    doc = insert_call.kwargs["document"]
    assert doc["role"] == "hr"
    assert doc["status"] == "pending"
    assert doc["email"] == "z@test.com"
    assert doc["name"] == "张三"
    assert "password_hash" in doc


@pytest.mark.asyncio
async def test_register_username_exists():
    """用户名已存在抛 USERNAME_EXISTS"""
    from app.core.exceptions import BizError
    svc = AuthService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value={"username": "zhangsan"})
    with pytest.raises(BizError) as exc:
        await svc.register(username="zhangsan", password="Pass1234")
    assert exc.value.code == 2002


@pytest.mark.asyncio
async def test_login_pending_rejected():
    """pending 状态拒绝登录"""
    svc = AuthService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value={
        "user_id": "u1", "username": "zhangsan",
        "password_hash": AuthService.hash_password("Pass1234"),
        "role": "hr", "status": "pending",
    })
    svc.redis = MagicMock()
    svc.redis.exists = AsyncMock(return_value=False)
    with pytest.raises(AuthError) as exc:
        await svc.login("zhangsan", "Pass1234")
    assert "待审批" in exc.value.message


@pytest.mark.asyncio
async def test_login_disabled_rejected():
    """disabled 状态拒绝登录"""
    svc = AuthService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value={
        "user_id": "u1", "username": "zhangsan",
        "password_hash": AuthService.hash_password("Pass1234"),
        "role": "hr", "status": "disabled",
    })
    svc.redis = MagicMock()
    svc.redis.exists = AsyncMock(return_value=False)
    with pytest.raises(AuthError) as exc:
        await svc.login("zhangsan", "Pass1234")
    assert "已禁用" in exc.value.message


@pytest.mark.asyncio
async def test_change_password_success():
    """修改自己密码成功"""
    svc = AuthService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value={
        "user_id": "u1", "username": "admin",
        "password_hash": AuthService.hash_password("Old1234"),
    })
    svc.users_coll.update_one = AsyncMock(return_value=MagicMock(modified_count=1))
    await svc.change_password(user_id="u1", old_password="Old1234", new_password="New1234")
    svc.users_coll.update_one.assert_called_once()


@pytest.mark.asyncio
async def test_change_password_old_wrong():
    """旧密码错误抛 AuthError"""
    svc = AuthService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value={
        "user_id": "u1", "username": "admin",
        "password_hash": AuthService.hash_password("Old1234"),
    })
    with pytest.raises(AuthError):
        await svc.change_password(user_id="u1", old_password="Wrong", new_password="New1234")
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv\Scripts\python.exe -m pytest tests/services/test_auth_service.py -v
```

预期：新增的 6 个测试 FAIL（register / change_password 未实现）

- [ ] **Step 3: 实现 register / change_password，login 加 status 校验**

修改 `backend/app/services/auth_service.py`：

在文件顶部 import 区追加：
```python
import uuid
from app.core.exceptions import AuthError, BizError
from app.core.response import CODE
```

（若 `BizError`、`CODE` 已导入则跳过；`AuthError` 已有）

在 `login` 方法中（校验密码之后、生成 token 之前）插入 status 校验：
```python
        # status 校验
        status = user_doc.get("status", "approved")
        if status == "pending":
            raise AuthError("账号待审批，请联系管理员")
        if status == "disabled":
            raise AuthError("账号已禁用，请联系管理员")
```

在 `refresh` 方法之后追加两个新方法：

```python
    async def register(self, username: str, password: str, email: str | None = None, name: str | None = None) -> dict:
        """HR 自助注册（status=pending, role=hr）

        入参:
            username: 用户名
            password: 明文密码
            email: 邮箱（可选）
            name: 显示名（可选）
        出参:
            {"user_id", "username", "status"}
        异常:
            BizError(CODE.USERNAME_EXISTS): 用户名已存在
        """
        existing = await self.users_coll.find_one({"username": username})
        if existing:
            raise BizError(CODE.USERNAME_EXISTS, "用户名已存在")
        user_id = f"u_{uuid.uuid4().hex[:12]}"
        now = datetime.now(timezone.utc).isoformat()
        doc = {
            "user_id": user_id,
            "username": username,
            "password_hash": self.hash_password(password),
            "email": email,
            "name": name or username,
            "role": "hr",
            "status": "pending",
            "created_at": now,
            "updated_at": now,
        }
        await self.users_coll.insert_one(doc)
        logger.info(f"用户注册申请: {username} (pending)")
        return {"user_id": user_id, "username": username, "status": "pending"}

    async def change_password(self, user_id: str, old_password: str, new_password: str) -> None:
        """用户修改自己密码

        入参:
            user_id: 用户 ID
            old_password: 旧密码
            new_password: 新密码
        异常:
            AuthError: 旧密码错误 / 用户不存在
        """
        doc = await self.users_coll.find_one({"user_id": user_id})
        if not doc or not self.verify_password(old_password, doc["password_hash"]):
            raise AuthError("旧密码错误")
        await self.users_coll.update_one(
            {"user_id": user_id},
            {"$set": {"password_hash": self.hash_password(new_password), "updated_at": datetime.now(timezone.utc).isoformat()}},
        )
        logger.info(f"用户 {user_id} 修改密码成功")
```

- [ ] **Step 4: 运行测试，确认全部通过**

```bash
.venv\Scripts\python.exe -m pytest tests/services/test_auth_service.py -v
```

预期：全部 PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/services/auth_service.py backend/tests/services/test_auth_service.py
git commit -m "feat(auth): AuthService 新增 register/change_password，login 增加 status 校验"
```

---

## Task 5: require_admin 依赖

**Files:**
- Modify: `backend/app/api/deps.py`
- Create: `backend/tests/api/test_deps.py`

**Interfaces:**
- Produces: require_admin 依赖函数

- [ ] **Step 1: 编写 require_admin 测试**

创建 `backend/tests/api/test_deps.py`：

```python
"""
文件名: tests/api/test_deps.py
创建时间: 2026-06-27
作者: TalentSense Team
功能描述: deps.require_admin 单元测试
"""
import pytest
from app.api.deps import require_admin
from app.core.exceptions import AuthError


@pytest.mark.asyncio
async def test_require_admin_pass():
    """admin 角色通过"""
    user = await require_admin({"user_id": "u1", "username": "admin", "role": "admin"})
    assert user["role"] == "admin"


@pytest.mark.asyncio
async def test_require_admin_reject_hr():
    """hr 角色被拒绝"""
    with pytest.raises(AuthError) as exc:
        await require_admin({"user_id": "u2", "username": "hr", "role": "hr"})
    assert "管理员" in exc.value.message
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv\Scripts\python.exe -m pytest tests/api/test_deps.py -v
```

预期：FAIL（require_admin 未定义）

- [ ] **Step 3: 实现 require_admin**

修改 `backend/app/api/deps.py`，在文件末尾追加：

```python
async def require_admin(user: dict = Depends(get_current_user)) -> dict:
    """要求 admin 角色，hr 调用抛 AuthError"""
    if user.get("role") != "admin":
        raise AuthError("需要管理员权限")
    return user
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv\Scripts\python.exe -m pytest tests/api/test_deps.py -v
```

预期：PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/deps.py backend/tests/api/test_deps.py
git commit -m "feat(auth): 新增 require_admin 依赖"
```

---

## Task 6: auth 路由扩展（register / password）

**Files:**
- Modify: `backend/app/api/auth.py`
- Modify: `backend/tests/api/test_auth_api.py`

**Interfaces:**
- Consumes: AuthService.register / change_password / RegisterRequest / ChangePasswordRequest

- [ ] **Step 1: 编写 register / password 路由测试**

修改 `backend/tests/api/test_auth_api.py`，追加：

```python
def test_register_api_success(client):
    """注册接口成功返回 pending"""
    from app.services.auth_service import AuthService
    AuthService._users_coll = None
    # 注入 mock
    from unittest.mock import MagicMock, AsyncMock
    svc_mock = MagicMock()
    svc_mock.register = AsyncMock(return_value={"user_id": "u_x", "username": "z", "status": "pending"})
    from app.api import auth as auth_api
    orig = auth_api.AuthService
    auth_api.AuthService = lambda: svc_mock
    try:
        r = client.post("/api/v1/auth/register", json={"username": "zhangsan", "password": "Pass1234", "email": "z@t.com", "name": "张三"})
        assert r.status_code == 200
        body = r.json()
        assert body["code"] == 0
        assert body["data"]["status"] == "pending"
    finally:
        auth_api.AuthService = orig


def test_change_password_api_success(client, auth_header):
    """修改密码接口成功"""
    from unittest.mock import MagicMock, AsyncMock
    svc_mock = MagicMock()
    svc_mock.change_password = AsyncMock(return_value=None)
    from app.api import auth as auth_api
    orig = auth_api.AuthService
    auth_api.AuthService = lambda: svc_mock
    try:
        r = client.put("/api/v1/auth/password", json={"old_password": "Old1234", "new_password": "New1234"}, headers=auth_header)
        assert r.status_code == 200
        assert r.json()["code"] == 0
    finally:
        auth_api.AuthService = orig
```

注：`auth_header` fixture 在 `tests/api/__init__.py` 或 `tests/conftest.py` 已有；若无则手动构造 `{"Authorization": "Bearer <token>"}`。

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv\Scripts\python.exe -m pytest tests/api/test_auth_api.py -v
```

预期：新增测试 FAIL（路由不存在）

- [ ] **Step 3: 实现 register / password 路由**

修改 `backend/app/api/auth.py`，在 `logout` 路由之后追加：

```python
from app.models.user import RegisterRequest, ChangePasswordRequest


@router.post("/register")
async def register(body: RegisterRequest):
    """HR 自助注册申请（status=pending）"""
    result = await AuthService().register(
        username=body.username, password=body.password,
        email=body.email, name=body.name,
    )
    return success(data=result)


@router.put("/password")
async def change_password(body: ChangePasswordRequest, user: dict = Depends(get_current_user)):
    """修改自己密码"""
    await AuthService().change_password(
        user_id=user["user_id"],
        old_password=body.old_password,
        new_password=body.new_password,
    )
    return success()
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv\Scripts\python.exe -m pytest tests/api/test_auth_api.py -v
```

预期：PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/auth.py backend/tests/api/test_auth_api.py
git commit -m "feat(auth): 新增 register/password 路由"
```

---

## Task 7: UserService 业务逻辑

**Files:**
- Create: `backend/app/services/user_service.py`
- Create: `backend/tests/services/test_user_service.py`

**Interfaces:**
- Consumes: MongoDB / AuthService.hash_password
- Produces: list_users / create_user / get_user / approve / reject / update_status / reset_password / delete

- [ ] **Step 1: 编写 UserService 测试**

创建 `backend/tests/services/test_user_service.py`：

```python
"""
文件名: tests/services/test_user_service.py
创建时间: 2026-06-27
作者: TalentSense Team
功能描述: UserService 单元测试
"""
import pytest
from unittest.mock import AsyncMock, MagicMock
from app.services.user_service import UserService
from app.core.exceptions import BizError, AuthError


def _make_user(user_id="u1", username="zhangsan", role="hr", status="pending"):
    return {
        "user_id": user_id, "username": username, "email": "z@t.com",
        "name": "张三", "role": role, "status": status,
        "password_hash": "hash", "created_at": "2026-06-27", "updated_at": "2026-06-27",
    }


@pytest.mark.asyncio
async def test_list_users():
    svc = UserService()
    svc.users_coll = MagicMock()
    cursor = MagicMock()
    cursor.skip = MagicMock(return_value=MagicMock(limit=MagicMock(return_value=cursor)))
    cursor.to_list = AsyncMock(return_value=[_make_user()])
    svc.users_coll.find = MagicMock(return_value=cursor)
    svc.users_coll.count_documents = AsyncMock(return_value=1)
    result = await svc.list_users(page=1, page_size=20)
    assert result["total"] == 1
    assert result["list"][0]["username"] == "zhangsan"
    # password_hash 不应出现在 list 项中
    assert "password_hash" not in result["list"][0]


@pytest.mark.asyncio
async def test_create_user_username_exists():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=_make_user())
    with pytest.raises(BizError) as exc:
        await svc.create_user(username="zhangsan", password="Pass1234", role="hr")
    assert exc.value.code == 2002


@pytest.mark.asyncio
async def test_create_user_success():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=None)
    svc.users_coll.insert_one = AsyncMock(return_value=MagicMock(inserted_id="oid"))
    result = await svc.create_user(username="newuser", password="Pass1234", role="hr", email="n@t.com", name="新")
    assert result["username"] == "newuser"
    assert result["status"] == "approved"


@pytest.mark.asyncio
async def test_approve_success():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=_make_user(status="pending"))
    svc.users_coll.update_one = AsyncMock(return_value=MagicMock(modified_count=1))
    await svc.approve(user_id="u1")
    svc.users_coll.update_one.assert_called_once()
    update_kwargs = svc.users_coll.update_one.call_args.kwargs
    assert update_kwargs["update"]["$set"]["status"] == "approved"


@pytest.mark.asyncio
async def test_reject_deletes_record():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=_make_user(status="pending"))
    svc.users_coll.delete_one = AsyncMock(return_value=MagicMock(deleted_count=1))
    await svc.reject(user_id="u1")
    svc.users_coll.delete_one.assert_called_once()


@pytest.mark.asyncio
async def test_update_status_success():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=_make_user(status="approved"))
    svc.users_coll.update_one = AsyncMock(return_value=MagicMock(modified_count=1))
    await svc.update_status(user_id="u1", status="disabled")
    update_kwargs = svc.users_coll.update_one.call_args.kwargs
    assert update_kwargs["update"]["$set"]["status"] == "disabled"


@pytest.mark.asyncio
async def test_reset_password_success():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=_make_user())
    svc.users_coll.update_one = AsyncMock(return_value=MagicMock(modified_count=1))
    await svc.reset_password(user_id="u1", new_password="NewPass1234")
    svc.users_coll.update_one.assert_called_once()


@pytest.mark.asyncio
async def test_delete_success():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=_make_user())
    svc.users_coll.delete_one = AsyncMock(return_value=MagicMock(deleted_count=1))
    await svc.delete(user_id="u1")
    svc.users_coll.delete_one.assert_called_once()


@pytest.mark.asyncio
async def test_cannot_delete_self():
    """管理员不能删除自己"""
    svc = UserService()
    with pytest.raises(AuthError):
        await svc.delete(user_id="u_self", current_user_id="u_self")


@pytest.mark.asyncio
async def test_user_not_found():
    svc = UserService()
    svc.users_coll = MagicMock()
    svc.users_coll.find_one = AsyncMock(return_value=None)
    with pytest.raises(BizError) as exc:
        await svc.approve(user_id="missing")
    assert exc.value.code == 2001
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv\Scripts\python.exe -m pytest tests/services/test_user_service.py -v
```

预期：FAIL（模块不存在）

- [ ] **Step 3: 实现 UserService**

创建 `backend/app/services/user_service.py`：

```python
"""
文件名: app/services/user_service.py
创建时间: 2026-06-27
作者: TalentSense Team
功能描述: 用户管理业务逻辑（列表/创建/审批/启用禁用/重置密码/删除）
入参: user_id / username / password / status / role
出参: 用户列表 / 用户详情 / 操作结果
对应 Business-Requirements F01 扩展
"""
import uuid
from datetime import datetime, timezone
from app.core.database import MongoDB
from app.core.exceptions import BizError, AuthError, NotFoundError
from app.core.response import CODE
from app.core.logger import logger
from app.services.auth_service import AuthService


class UserService:
    """用户管理服务"""

    def __init__(self):
        pass

    @property
    def users_coll(self):
        """延迟获取 MongoDB users collection"""
        if hasattr(self, "_users_coll"):
            return self._users_coll
        return MongoDB.db.users if MongoDB.db is not None else None

    @users_coll.setter
    def users_coll(self, value):
        """测试注入用"""
        self._users_coll = value

    async def list_users(self, page: int = 1, page_size: int = 20,
                         status: str | None = None, role: str | None = None,
                         keyword: str | None = None) -> dict:
        """分页列表查询

        入参:
            page: 页码
            page_size: 每页数量
            status: 状态筛选
            role: 角色筛选
            keyword: 用户名/姓名关键词
        出参:
            {list, total, page, page_size, total_pages}
        """
        query: dict = {}
        if status:
            query["status"] = status
        if role:
            query["role"] = role
        if keyword:
            query["$or"] = [
                {"username": {"$regex": keyword, "$options": "i"}},
                {"name": {"$regex": keyword, "$options": "i"}},
            ]
        cursor = self.users_coll.find(query, {"password_hash": 0, "_id": 0})
        list_data = await cursor.skip((page - 1) * page_size).limit(page_size).to_list(length=page_size)
        total = await self.users_coll.count_documents(query)
        return {
            "list": list_data,
            "total": total,
            "page": page,
            "page_size": page_size,
            "total_pages": (total + page_size - 1) // page_size if page_size > 0 else 0,
        }

    async def create_user(self, username: str, password: str, role: str = "hr",
                          email: str | None = None, name: str | None = None) -> dict:
        """管理员直接开号（status=approved）

        入参:
            username: 用户名
            password: 明文密码
            role: 角色 admin/hr
            email: 邮箱
            name: 显示名
        出参:
            用户信息
        异常:
            BizError(CODE.USERNAME_EXISTS): 用户名已存在
        """
        existing = await self.users_coll.find_one({"username": username})
        if existing:
            raise BizError(CODE.USERNAME_EXISTS, "用户名已存在")
        user_id = f"u_{uuid.uuid4().hex[:12]}"
        now = datetime.now(timezone.utc).isoformat()
        doc = {
            "user_id": user_id,
            "username": username,
            "password_hash": AuthService.hash_password(password),
            "email": email,
            "name": name or username,
            "role": role,
            "status": "approved",
            "created_at": now,
            "updated_at": now,
        }
        await self.users_coll.insert_one(doc)
        logger.info(f"管理员创建用户: {username} ({role})")
        doc.pop("password_hash", None)
        doc.pop("_id", None)
        return doc

    async def _get_or_404(self, user_id: str) -> dict:
        """获取用户，不存在抛 USER_NOT_FOUND"""
        doc = await self.users_coll.find_one({"user_id": user_id}, {"password_hash": 0, "_id": 0})
        if not doc:
            raise BizError(CODE.USER_NOT_FOUND, "用户不存在")
        return doc

    async def get_user(self, user_id: str) -> dict:
        """获取用户详情"""
        return await self._get_or_404(user_id)

    async def approve(self, user_id: str) -> None:
        """审批通过（pending → approved）"""
        await self._get_or_404(user_id)
        await self.users_coll.update_one(
            {"user_id": user_id},
            {"$set": {"status": "approved", "updated_at": datetime.now(timezone.utc).isoformat()}},
        )
        logger.info(f"用户 {user_id} 审批通过")

    async def reject(self, user_id: str) -> None:
        """拒绝申请（直接删除记录）"""
        await self._get_or_404(user_id)
        await self.users_coll.delete_one({"user_id": user_id})
        logger.info(f"用户 {user_id} 申请被拒绝（已删除）")

    async def update_status(self, user_id: str, status: str) -> None:
        """启用/禁用账号

        入参:
            user_id: 用户 ID
            status: approved / disabled
        """
        await self._get_or_404(user_id)
        await self.users_coll.update_one(
            {"user_id": user_id},
            {"$set": {"status": status, "updated_at": datetime.now(timezone.utc).isoformat()}},
        )
        logger.info(f"用户 {user_id} 状态变更为 {status}")

    async def reset_password(self, user_id: str, new_password: str) -> None:
        """管理员重置密码"""
        await self._get_or_404(user_id)
        await self.users_coll.update_one(
            {"user_id": user_id},
            {"$set": {"password_hash": AuthService.hash_password(new_password),
                      "updated_at": datetime.now(timezone.utc).isoformat()}},
        )
        logger.info(f"用户 {user_id} 密码已重置")

    async def delete(self, user_id: str, current_user_id: str | None = None) -> None:
        """删除账号（硬删除）

        入参:
            user_id: 待删除用户 ID
            current_user_id: 当前操作者 ID（防止自删）
        异常:
            AuthError: 试图删除自己
        """
        if current_user_id and user_id == current_user_id:
            raise AuthError("不能删除自己")
        await self._get_or_404(user_id)
        await self.users_coll.delete_one({"user_id": user_id})
        logger.info(f"用户 {user_id} 已删除")


user_service = UserService()
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv\Scripts\python.exe -m pytest tests/services/test_user_service.py -v
```

预期：PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/services/user_service.py backend/tests/services/test_user_service.py
git commit -m "feat(users): 新增 UserService 用户管理业务逻辑"
```

---

## Task 8: users 路由

**Files:**
- Create: `backend/app/api/users.py`
- Create: `backend/tests/api/test_users_api.py`

**Interfaces:**
- Consumes: UserService / require_admin / 用户模型

- [ ] **Step 1: 编写 users 路由测试**

创建 `backend/tests/api/test_users_api.py`：

```python
"""
文件名: tests/api/test_users_api.py
创建时间: 2026-06-27
作者: TalentSense Team
功能描述: users 路由 API 测试
"""
import pytest
from unittest.mock import MagicMock, AsyncMock
from fastapi.testclient import TestClient
from app.main import app


@pytest.fixture
def client():
    return TestClient(app)


@pytest.fixture
def admin_header():
    from app.api import deps
    async def _fake_admin():
        return {"user_id": "u_admin", "username": "admin", "role": "admin"}
    app.dependency_overrides[deps.require_admin] = _fake_admin
    app.dependency_overrides[deps.get_current_user] = _fake_admin
    yield {"Authorization": "Bearer fake"}
    app.dependency_overrides.clear()


def _patch_service(monkeypatch, **methods):
    from app.api import users as users_api
    svc = MagicMock()
    for name, ret in methods.items():
        setattr(svc, name, AsyncMock(return_value=ret))
    monkeypatch.setattr(users_api, "UserService", lambda: svc)
    return svc


def test_list_users_api(client, admin_header, monkeypatch):
    _patch_service(monkeypatch, list_users={
        "list": [{"user_id": "u1", "username": "zhangsan"}],
        "total": 1, "page": 1, "page_size": 20, "total_pages": 1,
    })
    r = client.get("/api/v1/users?page=1&page_size=20", headers=admin_header)
    assert r.status_code == 200
    body = r.json()
    assert body["code"] == 0
    assert body["data"]["total"] == 1


def test_create_user_api(client, admin_header, monkeypatch):
    _patch_service(monkeypatch, create_user={"user_id": "u2", "username": "new", "status": "approved"})
    r = client.post("/api/v1/users", json={"username": "new", "password": "Pass1234", "role": "hr"}, headers=admin_header)
    assert r.status_code == 200
    assert r.json()["data"]["username"] == "new"


def test_approve_api(client, admin_header, monkeypatch):
    svc = _patch_service(monkeypatch)
    r = client.put("/api/v1/users/u1/approve", headers=admin_header)
    assert r.status_code == 200
    svc.approve.assert_called_once_with(user_id="u1")


def test_reject_api(client, admin_header, monkeypatch):
    svc = _patch_service(monkeypatch)
    r = client.put("/api/v1/users/u1/reject", headers=admin_header)
    assert r.status_code == 200
    svc.reject.assert_called_once_with(user_id="u1")


def test_update_status_api(client, admin_header, monkeypatch):
    svc = _patch_service(monkeypatch)
    r = client.put("/api/v1/users/u1/status", json={"status": "disabled"}, headers=admin_header)
    assert r.status_code == 200
    svc.update_status.assert_called_once_with(user_id="u1", status="disabled")


def test_reset_password_api(client, admin_header, monkeypatch):
    svc = _patch_service(monkeypatch)
    r = client.put("/api/v1/users/u1/password", json={"new_password": "NewPass1234"}, headers=admin_header)
    assert r.status_code == 200
    svc.reset_password.assert_called_once_with(user_id="u1", new_password="NewPass1234")


def test_delete_user_api(client, admin_header, monkeypatch):
    svc = _patch_service(monkeypatch)
    r = client.delete("/api/v1/users/u1", headers=admin_header)
    assert r.status_code == 200
    svc.delete.assert_called_once_with(user_id="u1", current_user_id="u_admin")
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv\Scripts\python.exe -m pytest tests/api/test_users_api.py -v
```

预期：FAIL（路由不存在）

- [ ] **Step 3: 实现 users 路由**

创建 `backend/app/api/users.py`：

```python
"""
文件名: app/api/users.py
创建时间: 2026-06-27
作者: TalentSense Team
功能描述: 用户管理路由（全部 admin only），对应 API-Design 扩展
入参: HTTP 请求（路径参数 / 查询参数 / body）
出参: 统一响应 success/error
"""
from fastapi import APIRouter, Depends
from app.services.user_service import UserService
from app.api.deps import require_admin
from app.models.user import (
    CreateUserRequest, UpdateStatusRequest, ResetPasswordRequest,
)
from app.core.response import success

router = APIRouter()


@router.get("")
async def list_users(
    page: int = 1, page_size: int = 20,
    status: str | None = None, role: str | None = None,
    keyword: str | None = None,
    user: dict = Depends(require_admin),
):
    """用户列表（admin only）"""
    result = await UserService().list_users(
        page=page, page_size=page_size, status=status, role=role, keyword=keyword,
    )
    return success(data=result)


@router.post("")
async def create_user(body: CreateUserRequest, user: dict = Depends(require_admin)):
    """管理员直接开号（admin only）"""
    result = await UserService().create_user(
        username=body.username, password=body.password, role=body.role,
        email=body.email, name=body.name,
    )
    return success(data=result)


@router.get("/{user_id}")
async def get_user(user_id: str, user: dict = Depends(require_admin)):
    """用户详情（admin only）"""
    result = await UserService().get_user(user_id)
    return success(data=result)


@router.put("/{user_id}/approve")
async def approve(user_id: str, user: dict = Depends(require_admin)):
    """审批通过（admin only）"""
    await UserService().approve(user_id)
    return success()


@router.put("/{user_id}/reject")
async def reject(user_id: str, user: dict = Depends(require_admin)):
    """拒绝申请（admin only）"""
    await UserService().reject(user_id)
    return success()


@router.put("/{user_id}/status")
async def update_status(user_id: str, body: UpdateStatusRequest, user: dict = Depends(require_admin)):
    """启用/禁用（admin only）"""
    await UserService().update_status(user_id, body.status)
    return success()


@router.put("/{user_id}/password")
async def reset_password(user_id: str, body: ResetPasswordRequest, user: dict = Depends(require_admin)):
    """重置密码（admin only）"""
    await UserService().reset_password(user_id, body.new_password)
    return success()


@router.delete("/{user_id}")
async def delete_user(user_id: str, user: dict = Depends(require_admin)):
    """删除账号（admin only，不能删自己）"""
    await UserService().delete(user_id, current_user_id=user["user_id"])
    return success()
```

- [ ] **Step 4: 挂载路由到 main.py**

修改 `backend/app/main.py`，在 `from app.api import dashboard` 之后追加：

```python
from app.api import users
app.include_router(users.router, prefix=f"{settings.API_V1_PREFIX}/users", tags=["用户管理"])
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
.venv\Scripts\python.exe -m pytest tests/api/test_users_api.py -v
```

预期：PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/api/users.py backend/tests/api/test_users_api.py backend/app/main.py
git commit -m "feat(users): 新增用户管理路由并挂载"
```

---

## Task 9: 管理员自动初始化 + 接口权限加固

**Files:**
- Modify: `backend/app/main.py`
- Modify: `backend/app/api/resumes.py`
- Modify: `backend/app/api/email.py`

**Interfaces:**
- Consumes: settings.ADMIN_* / AuthService.hash_password / require_admin

- [ ] **Step 1: main.py lifespan 加 _ensure_admin**

修改 `backend/app/main.py` 的 `lifespan`，在 `# 重置遗留的 parsing 状态` 之前插入：

```python
    # 自动初始化管理员账号
    try:
        import uuid
        from app.services.auth_service import AuthService
        admin_username = settings.ADMIN_USERNAME
        exists = await MongoDB.db.users.find_one({"username": admin_username})
        if exists:
            logger.info(f"管理员账号 {admin_username} 已存在")
        else:
            user_id = f"u_{uuid.uuid4().hex[:12]}"
            now = datetime.now(timezone.utc).isoformat()
            await MongoDB.db.users.insert_one({
                "user_id": user_id,
                "username": admin_username,
                "password_hash": AuthService.hash_password(settings.ADMIN_PASSWORD),
                "email": settings.ADMIN_EMAIL,
                "name": "管理员",
                "role": "admin",
                "status": "approved",
                "created_at": now,
                "updated_at": now,
            })
            logger.info(f"管理员账号 {admin_username} 已自动创建")
    except Exception as e:
        logger.warning(f"管理员初始化失败: {e}")
```

并在文件顶部 `import json` 之下追加：
```python
import uuid
```
（若 main.py 已 import datetime，则 uuid 同样需要补）

- [ ] **Step 2: resumes.py delete 加 require_admin**

修改 `backend/app/api/resumes.py`，将 delete 路由签名改为：

```python
from app.api.deps import get_current_user, require_admin

@router.delete("/{resume_id}")
async def delete(resume_id: str, user: dict = Depends(require_admin)):
    """AC6.1-6.4: 删除简历（仅 admin）"""
    await ResumeService().delete(resume_id)
    return success()
```

- [ ] **Step 3: email.py config 接口加 require_admin**

修改 `backend/app/api/email.py`，将 GET/PUT `/config` 路由的依赖从 `get_current_user` 改为 `require_admin`。先读取该文件确认路由结构，再用 Edit 替换。

- [ ] **Step 4: 运行全部后端测试**

```bash
.venv\Scripts\python.exe -m pytest tests/ -q
```

预期：全部 PASS（若 resumes/email 测试因 mock 改动失败，需同步把 mock 改为 admin 角色）

- [ ] **Step 5: 提交**

```bash
git add backend/app/main.py backend/app/api/resumes.py backend/app/api/email.py backend/tests/
git commit -m "feat(auth): 管理员自动初始化 + 简历删除/SMTP配置加 admin 权限"
```

---

## Task 10: 前端类型与 API 扩展

**Files:**
- Modify: `frontend/src/types/auth.ts`
- Create: `frontend/src/types/user.ts`
- Modify: `frontend/src/api/auth.ts`
- Create: `frontend/src/api/user.ts`

- [ ] **Step 1: 扩展 auth 类型**

修改 `frontend/src/types/auth.ts`，将 `UserInfo.role` 类型细化为联合类型：

```typescript
export type UserRole = 'admin' | 'hr'
export type UserStatus = 'approved' | 'pending' | 'disabled'

export interface UserInfo {
  user_id: string
  username: string
  role: UserRole
  status?: UserStatus
  email?: string
  name?: string
}

export interface RegisterRequest {
  username: string
  password: string
  email?: string
  name?: string
}

export interface ChangePasswordRequest {
  old_password: string
  new_password: string
}
```

- [ ] **Step 2: 创建 user 类型**

创建 `frontend/src/types/user.ts`：

```typescript
/**
 * 文件名: types/user.ts
 * 创建时间: 2026-06-27
 * 作者: TalentSense Team
 * 功能描述: 用户管理类型定义
 */
import type { UserRole, UserStatus } from './auth'

export interface UserListItem {
  user_id: string
  username: string
  email: string | null
  name: string | null
  role: UserRole
  status: UserStatus
  created_at: string
  updated_at: string
}

export interface UserListResponse {
  list: UserListItem[]
  total: number
  page: number
  page_size: number
  total_pages: number
}

export interface CreateUserPayload {
  username: string
  password: string
  role: UserRole
  email?: string
  name?: string
}

export interface ListUsersParams {
  page?: number
  page_size?: number
  status?: UserStatus
  role?: UserRole
  keyword?: string
}
```

- [ ] **Step 3: 扩展 auth API**

修改 `frontend/src/api/auth.ts`，新增 register / changePassword：

```typescript
import type { RegisterRequest, ChangePasswordRequest } from '@/types/auth'

export function register(data: RegisterRequest): Promise<{ user_id: string; username: string; status: string }> {
  return request.post('/api/v1/auth/register', data)
}

export function changePassword(data: ChangePasswordRequest): Promise<void> {
  return request.put('/api/v1/auth/password', data)
}
```

（导入路径与现有 login 保持一致；request 实例来自 `@/api/request`）

- [ ] **Step 4: 创建 user API**

创建 `frontend/src/api/user.ts`：

```typescript
/**
 * 文件名: api/user.ts
 * 创建时间: 2026-06-27
 * 作者: TalentSense Team
 * 功能描述: 用户管理 API
 */
import request from './request'
import type { UserListResponse, UserListItem, CreateUserPayload, ListUsersParams, UserStatus } from '@/types/user'

export function listUsers(params: ListUsersParams): Promise<UserListResponse> {
  return request.get('/api/v1/users', { params })
}

export function createUser(data: CreateUserPayload): Promise<UserListItem> {
  return request.post('/api/v1/users', data)
}

export function approveUser(userId: string): Promise<void> {
  return request.put(`/api/v1/users/${userId}/approve`)
}

export function rejectUser(userId: string): Promise<void> {
  return request.put(`/api/v1/users/${userId}/reject`)
}

export function updateUserStatus(userId: string, status: UserStatus): Promise<void> {
  return request.put(`/api/v1/users/${userId}/status`, { status })
}

export function resetUserPassword(userId: string, newPassword: string): Promise<void> {
  return request.put(`/api/v1/users/${userId}/password`, { new_password: newPassword })
}

export function deleteUser(userId: string): Promise<void> {
  return request.delete(`/api/v1/users/${userId}`)
}
```

- [ ] **Step 5: 提交**

```bash
git add frontend/src/types/auth.ts frontend/src/types/user.ts frontend/src/api/auth.ts frontend/src/api/user.ts
git commit -m "feat(frontend): 新增用户管理类型与 API"
```

---

## Task 11: 注册对话框 + 登录页集成

**Files:**
- Create: `frontend/src/components/auth/RegisterDialog.vue`
- Modify: `frontend/src/views/Login.vue`

- [ ] **Step 1: 创建 RegisterDialog**

创建 `frontend/src/components/auth/RegisterDialog.vue`：

```vue
<!--
  文件名: components/auth/RegisterDialog.vue
  创建时间: 2026-06-27
  作者: TalentSense Team
  功能描述: 注册申请对话框（HR 自助申请，提交后 status=pending）
-->
<template>
  <el-dialog v-model="visible" title="申请账号" width="420px" :close-on-click-modal="false">
    <el-form ref="formRef" :model="form" :rules="rules" label-position="top">
      <el-form-item label="用户名" prop="username">
        <el-input v-model="form.username" placeholder="3-20 字符" />
      </el-form-item>
      <el-form-item label="密码" prop="password">
        <el-input v-model="form.password" type="password" show-password placeholder="8-32 字符" />
      </el-form-item>
      <el-form-item label="邮箱" prop="email">
        <el-input v-model="form.email" placeholder="可选" />
      </el-form-item>
      <el-form-item label="姓名" prop="name">
        <el-input v-model="form.name" placeholder="可选" />
      </el-form-item>
    </el-form>
    <template #footer>
      <el-button @click="visible = false">取消</el-button>
      <el-button type="primary" :loading="loading" @click="handleSubmit">提交申请</el-button>
    </template>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { ElMessage, type FormInstance, type FormRules } from 'element-plus'
import { register } from '@/api/auth'

const visible = ref(false)
const loading = ref(false)
const formRef = ref<FormInstance>()

const form = reactive({
  username: '',
  password: '',
  email: '',
  name: '',
})

const rules: FormRules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 20, message: '3-20 字符', trigger: 'blur' },
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 8, max: 32, message: '8-32 字符', trigger: 'blur' },
  ],
}

function open(): void {
  form.username = ''
  form.password = ''
  form.email = ''
  form.name = ''
  visible.value = true
}

async function handleSubmit(): Promise<void> {
  if (!formRef.value) return
  try {
    await formRef.value.validate()
  } catch {
    return
  }
  loading.value = true
  try {
    await register({
      username: form.username.trim(),
      password: form.password,
      email: form.email || undefined,
      name: form.name || undefined,
    })
    ElMessage.success('申请已提交，待管理员审批')
    visible.value = false
  } catch (err) {
    const msg = err instanceof Error ? err.message : '申请失败'
    ElMessage.error(msg)
  } finally {
    loading.value = false
  }
}

defineExpose({ open })
</script>
```

- [ ] **Step 2: Login.vue 加"申请账号"链接**

修改 `frontend/src/views/Login.vue`：
1. 在 `<script setup>` 中导入 RegisterDialog 并创建 ref：

```typescript
import RegisterDialog from '@/components/auth/RegisterDialog.vue'
const registerDialogRef = ref<InstanceType<typeof RegisterDialog>>()
```

2. 在模板的 `</el-form>` 之后、`<p class="page-login__footnote">` 之前插入：

```vue
        <p class="page-login__register">
          <a href="javascript:void(0)" @click="registerDialogRef?.open()">申请账号</a>
        </p>
        <RegisterDialog ref="registerDialogRef" />
```

3. 在 `<style>` 中追加：

```scss
  &__register {
    margin-top: var(--space-4);
    text-align: center;
    font-size: var(--text-sm);

    a {
      color: var(--color-accent);
      text-decoration: none;
      &:hover { text-decoration: underline; }
    }
  }
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/auth/RegisterDialog.vue frontend/src/views/Login.vue
git commit -m "feat(frontend): 登录页新增申请账号入口"
```

---

## Task 12: UserList 用户管理页

**Files:**
- Create: `frontend/src/views/UserList.vue`

- [ ] **Step 1: 创建 UserList.vue**

创建 `frontend/src/views/UserList.vue`：

```vue
<!--
  文件名: views/UserList.vue
  创建时间: 2026-06-27
  作者: TalentSense Team
  功能描述: 用户管理页（仅 admin 可见）
    - 分页表格 + 状态/角色/关键词筛选
    - 创建账号 / 审批 / 启用禁用 / 重置密码 / 删除
-->
<template>
  <div class="page-users">
    <div class="page-users__toolbar">
      <div class="page-users__filters">
        <el-select v-model="filters.status" placeholder="状态" clearable style="width: 120px" @change="loadList">
          <el-option label="待审批" value="pending" />
          <el-option label="正常" value="approved" />
          <el-option label="禁用" value="disabled" />
        </el-select>
        <el-select v-model="filters.role" placeholder="角色" clearable style="width: 120px" @change="loadList">
          <el-option label="管理员" value="admin" />
          <el-option label="HR" value="hr" />
        </el-select>
        <el-input v-model="filters.keyword" placeholder="用户名/姓名" clearable style="width: 200px" @keyup.enter="loadList" @clear="loadList" />
        <el-button @click="loadList">查询</el-button>
      </div>
      <el-button type="primary" @click="openCreate">创建账号</el-button>
    </div>

    <el-table :data="list" v-loading="loading" class="page-users__table">
      <el-table-column prop="username" label="用户名" width="140" />
      <el-table-column prop="name" label="姓名" width="120" />
      <el-table-column prop="email" label="邮箱" min-width="180" />
      <el-table-column label="角色" width="100">
        <template #default="{ row }">
          <el-tag :type="row.role === 'admin' ? 'danger' : 'info'" size="small">
            {{ row.role === 'admin' ? '管理员' : 'HR' }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column label="状态" width="100">
        <template #default="{ row }">
          <el-tag :type="statusType(row.status)" size="small">{{ statusText(row.status) }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="created_at" label="创建时间" width="180" />
      <el-table-column label="操作" width="280" fixed="right">
        <template #default="{ row }">
          <template v-if="row.status === 'pending'">
            <el-button size="small" type="success" @click="handleApprove(row)">通过</el-button>
            <el-button size="small" type="danger" @click="handleReject(row)">拒绝</el-button>
          </template>
          <template v-else>
            <el-button v-if="row.status === 'approved'" size="small" @click="handleToggleStatus(row, 'disabled')">禁用</el-button>
            <el-button v-else size="small" type="success" @click="handleToggleStatus(row, 'approved')">启用</el-button>
            <el-button size="small" @click="openReset(row)">重置密码</el-button>
            <el-button size="small" type="danger" @click="handleDelete(row)">删除</el-button>
          </template>
        </template>
      </el-table-column>
    </el-table>

    <el-pagination
      v-model:current-page="pagination.page"
      v-model:page-size="pagination.page_size"
      :total="pagination.total"
      :page-sizes="[10, 20, 50]"
      layout="total, sizes, prev, pager, next"
      class="page-users__pagination"
      @current-change="loadList"
      @size-change="loadList"
    />

    <!-- 创建账号对话框 -->
    <el-dialog v-model="createVisible" title="创建账号" width="440px">
      <el-form ref="createFormRef" :model="createForm" :rules="createRules" label-position="top">
        <el-form-item label="用户名" prop="username">
          <el-input v-model="createForm.username" />
        </el-form-item>
        <el-form-item label="密码" prop="password">
          <el-input v-model="createForm.password" type="password" show-password />
        </el-form-item>
        <el-form-item label="邮箱" prop="email">
          <el-input v-model="createForm.email" />
        </el-form-item>
        <el-form-item label="姓名" prop="name">
          <el-input v-model="createForm.name" />
        </el-form-item>
        <el-form-item label="角色" prop="role">
          <el-radio-group v-model="createForm.role">
            <el-radio value="hr">HR</el-radio>
            <el-radio value="admin">管理员</el-radio>
          </el-radio-group>
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="createVisible = false">取消</el-button>
        <el-button type="primary" :loading="submitLoading" @click="handleCreate">创建</el-button>
      </template>
    </el-dialog>

    <!-- 重置密码对话框 -->
    <el-dialog v-model="resetVisible" title="重置密码" width="400px">
      <el-form ref="resetFormRef" :model="resetForm" :rules="resetRules" label-position="top">
        <el-form-item label="新密码" prop="new_password">
          <el-input v-model="resetForm.new_password" type="password" show-password />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="resetVisible = false">取消</el-button>
        <el-button type="primary" :loading="submitLoading" @click="handleReset">确定</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { ElMessage, ElMessageBox, type FormInstance, type FormRules } from 'element-plus'
import {
  listUsers, createUser, approveUser, rejectUser,
  updateUserStatus, resetUserPassword, deleteUser,
} from '@/api/user'
import type { UserListItem, UserStatus, UserRole } from '@/types/user'

const list = ref<UserListItem[]>([])
const loading = ref(false)
const submitLoading = ref(false)

const filters = reactive({
  status: '' as UserStatus | '',
  role: '' as UserRole | '',
  keyword: '',
})

const pagination = reactive({ page: 1, page_size: 20, total: 0 })

const createVisible = ref(false)
const createFormRef = ref<FormInstance>()
const createForm = reactive({
  username: '', password: '', email: '', name: '', role: 'hr' as UserRole,
})
const createRules: FormRules = {
  username: [{ required: true, min: 3, max: 20, message: '3-20 字符', trigger: 'blur' }],
  password: [{ required: true, min: 8, max: 32, message: '8-32 字符', trigger: 'blur' }],
  role: [{ required: true, message: '请选择角色', trigger: 'change' }],
}

const resetVisible = ref(false)
const resetFormRef = ref<FormInstance>()
const resetForm = reactive({ new_password: '' })
const resetTargetId = ref('')
const resetRules: FormRules = {
  new_password: [{ required: true, min: 8, max: 32, message: '8-32 字符', trigger: 'blur' }],
}

function statusType(s: UserStatus): 'warning' | 'success' | 'info' {
  return s === 'pending' ? 'warning' : s === 'approved' ? 'success' : 'info'
}
function statusText(s: UserStatus): string {
  return s === 'pending' ? '待审批' : s === 'approved' ? '正常' : '禁用'
}

async function loadList(): Promise<void> {
  loading.value = true
  try {
    const data = await listUsers({
      page: pagination.page,
      page_size: pagination.page_size,
      status: filters.status || undefined,
      role: filters.role || undefined,
      keyword: filters.keyword || undefined,
    })
    list.value = data.list
    pagination.total = data.total
  } catch (err) {
    ElMessage.error(err instanceof Error ? err.message : '加载失败')
  } finally {
    loading.value = false
  }
}

function openCreate(): void {
  createForm.username = ''
  createForm.password = ''
  createForm.email = ''
  createForm.name = ''
  createForm.role = 'hr'
  createVisible.value = true
}

async function handleCreate(): Promise<void> {
  if (!createFormRef.value) return
  try { await createFormRef.value.validate() } catch { return }
  submitLoading.value = true
  try {
    await createUser({
      username: createForm.username.trim(),
      password: createForm.password,
      role: createForm.role,
      email: createForm.email || undefined,
      name: createForm.name || undefined,
    })
    ElMessage.success('创建成功')
    createVisible.value = false
    await loadList()
  } catch (err) {
    ElMessage.error(err instanceof Error ? err.message : '创建失败')
  } finally {
    submitLoading.value = false
  }
}

async function handleApprove(row: UserListItem): Promise<void> {
  try {
    await ElMessageBox.confirm(`确认通过 ${row.username} 的申请？`, '审批', { type: 'warning' })
    await approveUser(row.user_id)
    ElMessage.success('已通过')
    await loadList()
  } catch { /* cancel */ }
}

async function handleReject(row: UserListItem): Promise<void> {
  try {
    await ElMessageBox.confirm(`确认拒绝 ${row.username} 的申请？此操作不可恢复`, '拒绝', { type: 'warning' })
    await rejectUser(row.user_id)
    ElMessage.success('已拒绝')
    await loadList()
  } catch { /* cancel */ }
}

async function handleToggleStatus(row: UserListItem, status: UserStatus): Promise<void> {
  const action = status === 'disabled' ? '禁用' : '启用'
  try {
    await ElMessageBox.confirm(`确认${action} ${row.username}？`, action, { type: 'warning' })
    await updateUserStatus(row.user_id, status)
    ElMessage.success(`已${action}`)
    await loadList()
  } catch { /* cancel */ }
}

function openReset(row: UserListItem): void {
  resetTargetId.value = row.user_id
  resetForm.new_password = ''
  resetVisible.value = true
}

async function handleReset(): Promise<void> {
  if (!resetFormRef.value) return
  try { await resetFormRef.value.validate() } catch { return }
  submitLoading.value = true
  try {
    await resetUserPassword(resetTargetId.value, resetForm.new_password)
    ElMessage.success('密码已重置')
    resetVisible.value = false
  } catch (err) {
    ElMessage.error(err instanceof Error ? err.message : '重置失败')
  } finally {
    submitLoading.value = false
  }
}

async function handleDelete(row: UserListItem): Promise<void> {
  try {
    await ElMessageBox.confirm(`确认删除 ${row.username}？此操作不可恢复`, '删除', { type: 'error' })
    await deleteUser(row.user_id)
    ElMessage.success('已删除')
    await loadList()
  } catch { /* cancel */ }
}

onMounted(() => { loadList() })
</script>

<style scoped lang="scss">
.page-users {
  &__toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-4);
  }
  &__filters {
    display: flex;
    gap: var(--space-3);
    align-items: center;
  }
  &__table {
    width: 100%;
  }
  &__pagination {
    margin-top: var(--space-4);
    justify-content: flex-end;
  }
}
</style>
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/views/UserList.vue
git commit -m "feat(frontend): 新增用户管理页 UserList"
```

---

## Task 13: Layout 菜单 + 路由守卫

**Files:**
- Modify: `frontend/src/router/index.ts`
- Modify: `frontend/src/views/Layout.vue`

- [ ] **Step 1: 路由加 /users + 守卫扩展**

修改 `frontend/src/router/index.ts`，在 `settings` 路由之后追加：

```typescript
      {
        path: 'users',
        name: 'UserList',
        component: () => import('@/views/UserList.vue'),
        meta: { title: '用户管理', requireAdmin: true },
      },
```

并在 `router.beforeEach` 中 `return true` 之前插入 admin 守卫：

```typescript
  // admin 路由权限校验
  if (to.meta.requireAdmin === true && auth.user?.role !== 'admin') {
    return { path: '/workbench' }
  }
```

- [ ] **Step 2: Layout 加 admin 菜单项与标识**

修改 `frontend/src/views/Layout.vue`：

1. 在 `<script setup>` 的 icons import 中追加 `UserFilled`：

```typescript
import {
  ChatDotRound, Document, Connection, DataLine, Setting,
  Fold, Expand, SwitchButton, UserFilled,
} from '@element-plus/icons-vue'
```

2. 在模板的 `<el-menu-item index="/settings">` 之后插入：

```vue
        <el-menu-item v-if="authStore.user?.role === 'admin'" index="/users">
          <el-icon><UserFilled /></el-icon>
          <template #title>用户管理</template>
        </el-menu-item>
```

3. 在顶栏 `<span class="header__username">` 之后插入管理员标识：

```vue
          <el-tag v-if="authStore.user?.role === 'admin'" type="danger" size="small" effect="plain">管理员</el-tag>
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/router/index.ts frontend/src/views/Layout.vue
git commit -m "feat(frontend): 路由守卫 + Layout admin 菜单与标识"
```

---

## Task 14: 联调验证

**Files:** 无（仅运行验证）

- [ ] **Step 1: 运行后端全部测试**

```bash
cd backend
.venv\Scripts\python.exe -m pytest tests/ -q
```

预期：全部 PASS

- [ ] **Step 2: 启动后端，验证管理员自动初始化**

```bash
.venv\Scripts\python.exe -m uvicorn app.main:app --port 8001
```

查看日志：`管理员账号 admin 已自动创建` 或 `管理员账号 admin 已存在`

- [ ] **Step 3: 实测 admin 登录 + 用户管理接口**

```bash
# 登录
curl.exe -X POST http://127.0.0.1:8001/api/v1/auth/login -H "Content-Type: application/json" -d "@login.json"

# 申请注册（HR）
curl.exe -X POST http://127.0.0.1:8001/api/v1/auth/register -H "Content-Type: application/json" -d "@reg.json"

# 用户列表
curl.exe http://127.0.0.1:8001/api/v1/users -H "Authorization: Bearer <TOKEN>"

# 审批
curl.exe -X PUT http://127.0.0.1:8001/api/v1/users/<ID>/approve -H "Authorization: Bearer <TOKEN>"
```

- [ ] **Step 4: 验证 HR 无法删除简历**

用 HR 账号登录后调用 `DELETE /api/v1/resumes/{id}`，预期返回权限不足错误。

- [ ] **Step 5: 前端联调**

启动前端 `npm run dev`，验证：
1. 登录页有"申请账号"链接
2. admin 登录后侧边栏显示"用户管理"
3. HR 登录后侧边栏不显示"用户管理"，访问 /users 跳转 /workbench
4. admin 顶栏显示"管理员"标签
5. 用户管理页 CRUD 全流程可用

- [ ] **Step 6: 最终提交**

```bash
git add .
git commit -m "test: 联调验证通过 - 注册/权限/用户管理全流程"
```

---

## 自检

- ✅ Spec 覆盖：14 个 Task 覆盖设计文档第 3-8 节全部 API 与前端功能
- ✅ 无占位符：每个 Step 含完整代码
- ✅ 类型一致：后端 Pydantic 模型 ↔ 前端 TS 类型字段对齐
- ✅ TDD：每个 Service/路由先写测试再实现
- ✅ 频繁提交：每个 Task 末尾有 commit
